{
  "name": "waddle-dashboard",
  "version": "0.1.0",
  "description": "Evidence.dev dashboard for WaddleML experiment tracking — the SQL-native replacement for the built-in dashboard.",
  "type": "module",
  "scripts": {
    "build": "evidence build",
    "build:strict": "evidence build:strict",
    "dev": "evidence dev --open /",
    "sources": "evidence sources",
    "preview": "evidence preview"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@evidence-dev/core-components": "^5.4.2",
    "@evidence-dev/duckdb": "^2.0.1",
    "@evidence-dev/evidence": "^40.1.8"
  },
  "overrides": {
    "axios": "^1.7.4",
    "typescript": "^5.6.0"
  }
}
